[mysqld]
# 服务器唯一ID（必须与主库不同）
server-id = 2
# 启用二进制日志
log-bin = mysql-bin
# 二进制日志格式
binlog-format = ROW
# 启用GTID模式
gtid-mode = ON
# 强制GTID一致性
enforce-gtid-consistency = ON
# 记录从库更新
log-slave-updates = ON
# 只读模式
read-only = ON
