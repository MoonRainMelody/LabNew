<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.lab2.mapper.UserMapper">

    <insert id="save" parameterType="org.example.lab2.entity.User">
        INSERT INTO t_user (user_id, username, email, create_time)
        VALUES (#{userId}, #{username}, #{email}, #{createTime})
    </insert>

    <select id="findById" resultType="org.example.lab2.entity.User">
        SELECT user_id, username, email, create_time
        FROM t_user
        WHERE user_id = #{userId}
    </select>

    <select id="findAll" resultType="org.example.lab2.entity.User">
        SELECT user_id, username, email, create_time
        FROM t_user
    </select>

</mapper>
